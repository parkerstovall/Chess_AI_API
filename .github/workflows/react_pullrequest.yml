name: React PR

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./client/
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
      - run: npm install
      - run: npm run build
      - run: npx tsc --build --clean
      - run: npx prettier -c "./**/{app,components}/**/*{.ts,.tsx}"
      - run: rm -rf node_modules
